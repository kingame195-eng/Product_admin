# Production Environment Variables
# ⚠️ IMPORTANT: Change these values before deploying!

# Environment
NODE_ENV=production

# Database - MongoDB Atlas
# Format: mongodb+srv://USERNAME:PASSWORD@CLUSTER.mongodb.net/DATABASE?retryWrites=true&w=majority
# Get this from: MongoDB Atlas → Clusters → Connect → Drivers
MONGO_URI=mongodb+srv://admin:YOUR_PASSWORD@product-admin.xxxxx.mongodb.net/product_admin?retryWrites=true&w=majority

# JWT Secrets - MUST be long random strings (32+ characters)
# Generate at: https://www.uuidgenerator.net/ or use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-change-this-in-production!@#$%^&*()
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars-change-this-too!@#$%^&*()

# Token Expiration
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# API Configuration
PORT=5001
API_PREFIX=/api/v1

# CORS - Only allow your frontend domain
# Set to your actual domain: https://yourdomain.com or https://app.vercel.app
CORS_ORIGIN=https://yourdomain.com

# Logging (optional)
LOG_LEVEL=info
